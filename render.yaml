services:
  - type: web
    name: skot-catan
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000 # Render's default but good to be explicit or let it be dynamic
